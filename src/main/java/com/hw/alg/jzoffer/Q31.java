package com.hw.alg.jzoffer;

import java.util.Stack;

public class Q31 {


    /**
     * push表示栈的压入顺序, 判断pop是否是该栈的一个压出顺序
     * @param push
     * @param pop
     * @return
     */
    public boolean isPopOrder(int[] push, int[] pop){
        boolean result = false;
        Stack<Integer> DataStack = new Stack<>();

        for (int i=0; i < pop.length; i++){

        }

        return false;
    }
}
